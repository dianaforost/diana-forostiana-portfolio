import React from "react";
import LoaderIcon from "../../icons/LoaderIcon";

const Loader = () => {
  return (
    <LoaderIcon className="animate-spin fill-current" width={16} height={16} />
  );
};

export default Loader;
